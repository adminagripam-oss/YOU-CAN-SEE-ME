
  # Analytics Dashboard with Cross-Filtering (Community)

  This is a code bundle for Analytics Dashboard with Cross-Filtering (Community). The original project is available at https://www.figma.com/design/F7gCADbukNRXh5ptskyU3D/Analytics-Dashboard-with-Cross-Filtering--Community-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  